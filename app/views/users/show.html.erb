<h1 class="text-center mt-4"><%= @aUser.username %>'s Profile</h1>

<%= gravatar_for(@aUser, size: 200) %>

<div class="text-center mt-4">
<%= link_to "Edit your profile", edit_user_path(@aUser), class: "btn btn-outline-info" %>
</div>

<h3 class="text-center t-4"><%= @aUser.username %>'s Articles</h3>
<%= render "articles/article-list.html.erb" %>
